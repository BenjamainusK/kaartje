backend:
  name: git-gateway
  branch: master
media_folder: static/img

collections:
  - name: "home"
    label: "home"
    folder: "/index.html"
    filter:
      field: section
      value: nl
    fields:
      - {label: "section", name: "section"}
